name: '[Docs] Build docs and upload artifacts'
on:
  push:
    branches: [ 'main', 'doc_*', 'v1'] 
  release:
    types: [ 'published' ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  notebooks:
    name: "Build and run the notebooks for the docs"
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.8

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -U .
          python -m pip install -r ./docs/requirements-doc.txt


      # - name: Create diagrams with pyreverse
      #   run: |
      #     pyreverse -k  -o svg -p siibra ./siibra --colorized --all-ancestors --output-directory ./docs/_static

      - name: Build the docs and execute the notebooks
        run: |
          sphinx-build docs ./docs/_build/

      - uses: actions/upload-artifact@v4
        with:
          name: sphinx-docs-built-in-github-${{ github.sha }}
          path: docs

      - name: Trigger RTDs build
        uses: dfm/rtds-action@v1
        with:
          webhook_url: ${{ secrets.RTDS_WEBHOOK_URL }}
          webhook_token: ${{ secrets.RTDS_WEBHOOK_TOKEN }}
          commit_ref: ${{ github.ref }}

  cleanup:
    name: "Clean up build artifacts and pip cache"
    runs-on: self-hosted
    needs: notebooks
    # Always run cleanup, even if the build job failed
    if: always()
    steps:
      - name: Clear siibra cache
        continue-on-error: true
        run: |
          python -c "import siibra; siibra.cache.clear()"

      - name: Remove checked-out repository files
        continue-on-error: true
        run: |
          rm -rf ${{ github.workspace }}/*
